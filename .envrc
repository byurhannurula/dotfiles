# Loads the dev shell (nixfmt, sops, gitleaks, nvd...) on `cd` into this repo.
# One-time:  direnv allow
#
# Note: deliberately NOT running `git pull` here, unlike notthebee's .envrc —
# a silent network call on every cd is a surprising side effect.
use flake
